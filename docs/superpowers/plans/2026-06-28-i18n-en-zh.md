# 多语言支持（en / zh-CN）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 WebMail（ASP.NET Core 8 Razor Pages）添加英文/简体中文双语支持，浏览器自动检测 + 导航栏手动切换（Cookie 持久化），覆盖界面文字与系统消息。

**Architecture:** 采用 ASP.NET Core 官方本地化：`AddLocalization` + `IStringLocalizer<SharedResource>` + 单一共享 `.resx`，配 `RequestLocalizationMiddleware`。**视图静态文字**通过 `_ViewImports.cshtml` 全局注入的 `@L["key"]` 本地化；**页面模型的动态消息**通过注入的 `IStringLocalizer<SharedResource>` 本地化；**服务层**保持文化无关——返回稳定的资源 **key**，由页面模型负责翻译。

**Tech Stack:** .NET 8, ASP.NET Core Razor Pages, `Microsoft.Extensions.Localization`（框架自带，无需新增 NuGet 包）, xUnit + EF InMemory（既有测试栈）。

## Global Constraints

- 目标框架 `net8.0`；`Nullable` 与 `ImplicitUsings` 已启用——新代码必须可空安全。
- **不新增 NuGet 依赖**：本地化 API 来自已引用的 `Microsoft.AspNetCore.App` 框架。
- 支持语言固定为两种：`en` 与 `zh-CN`；**默认回退 `en`**。
- 资源 key 用语义化英文标识符、点号分组（如 `Nav.Logout`）；**禁止**用中文原文作 key。
- `SharedResource.en.resx` 与 `SharedResource.zh-CN.resx` 的 key 集合必须**完全一致**（由 parity 测试守护）。
- **不翻译**：邮件主题/正文、买家/供应商名称、邮箱地址；枚举显示值（`@buyer.EmailStatus` 等渲染的英文枚举名）；`.cs` 注释；异常/日志文本；`IdentitySeeder` 中作为种子数据写库的 `DisplayName = "管理员"`。
- 服务层（`UserAdminService`、`BuyerRuleService`）**不得**注入 `IStringLocalizer`——只返回 key 字符串。
- 提交粒度：每个 Task 结束时 `dotnet build` 与 `dotnet test` 必须全绿后再 commit。

---

## 关键约定（所有 Task 通用）

### 资源 key 引用方式
- 视图：经 `_ViewImports.cshtml` 全局注入 `L`，直接 `@L["Nav.Logout"]`；带参 `@L["Admin.Users.Title", L[Model.RoleNounKey].Value]`。
- 页面模型：构造函数注入 `IStringLocalizer<SharedResource> loc`，`ErrorMessage = loc["Login.InvalidCredentials"]`；带参 `loc["OAuth.AuthorizationFailed", error]`。
- 服务层：返回 key 字面量字符串（如 `return new(false, "User.UserNameRequired");`）。

### 测试用本地化桩
所有受影响的页面模型构造函数都会新增一个 `IStringLocalizer<SharedResource>` 参数。测试统一用 `TestLocalizer.Shared`（Task 1 创建）——一个**回显 key** 的桩：`loc["X"]` 返回 `"X"`。因此页面模型测试断言的是 **key 字符串**，与翻译文本解耦。

---

## Task 1: 本地化基础设施

**Files:**
- Create: `src/WebMail/SharedResource.cs`
- Create: `src/WebMail/Services/Localization/LocalizationConfig.cs`
- Create: `src/WebMail/Resources/SharedResource.en.resx`（本任务仅建空壳，含 1 个探针 key）
- Create: `src/WebMail/Resources/SharedResource.zh-CN.resx`（同上）
- Modify: `src/WebMail/Program.cs`
- Modify: `src/WebMail/Pages/_ViewImports.cshtml`
- Create: `tests/WebMail.Tests/TestLocalizer.cs`
- Test: `tests/WebMail.Tests/LocalizationConfigTests.cs`

**Interfaces:**
- Produces:
  - `namespace WebMail; public sealed class SharedResource {}`（本地化锚点类）
  - `namespace WebMail.Services.Localization; public static class LocalizationConfig`：
    - `public static readonly string[] SupportedCultureNames = { "en", "zh-CN" };`
    - `public const string DefaultCultureName = "en";`
    - `public static RequestLocalizationOptions Build()` — 返回配置好的 options（默认 en、支持 en+zh-CN、`SupportedCultures`/`SupportedUICultures` 一致）。
  - `tests/.../TestLocalizer.cs`：`public static class TestLocalizer { public static IStringLocalizer<SharedResource> Shared { get; } }`（回显 key 的桩）。

- [ ] **Step 1: 写失败测试 `LocalizationConfigTests`**

```csharp
using System.Globalization;
using WebMail.Services.Localization;
using Xunit;

namespace WebMail.Tests;

public sealed class LocalizationConfigTests
{
    [Fact]
    public void DefaultCultureIsEnglish()
    {
        var options = LocalizationConfig.Build();
        Assert.Equal("en", options.DefaultRequestCulture.Culture.Name);
        Assert.Equal("en", options.DefaultRequestCulture.UICulture.Name);
    }

    [Fact]
    public void SupportsEnglishAndSimplifiedChineseOnly()
    {
        var options = LocalizationConfig.Build();
        var ui = options.SupportedUICultures!.Select(c => c.Name).OrderBy(n => n).ToArray();
        var cultures = options.SupportedCultures!.Select(c => c.Name).OrderBy(n => n).ToArray();
        Assert.Equal(new[] { "en", "zh-CN" }, ui);
        Assert.Equal(new[] { "en", "zh-CN" }, cultures);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `dotnet test tests/WebMail.Tests --filter LocalizationConfigTests`
Expected: 编译失败（`LocalizationConfig` 不存在）。

- [ ] **Step 3: 创建 `SharedResource.cs`**

```csharp
namespace WebMail;

/// <summary>共享本地化资源的锚点类型，对应 Resources/SharedResource.*.resx。</summary>
public sealed class SharedResource
{
}
```

- [ ] **Step 4: 创建 `LocalizationConfig.cs`**

```csharp
using System.Globalization;
using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Localization;

namespace WebMail.Services.Localization;

public static class LocalizationConfig
{
    public const string DefaultCultureName = "en";
    public static readonly string[] SupportedCultureNames = { "en", "zh-CN" };

    public static RequestLocalizationOptions Build()
    {
        var cultures = SupportedCultureNames.Select(c => new CultureInfo(c)).ToList();
        return new RequestLocalizationOptions
        {
            DefaultRequestCulture = new RequestCulture(DefaultCultureName, DefaultCultureName),
            SupportedCultures = cultures,
            SupportedUICultures = cultures,
        };
        // 默认 Provider 顺序为 [QueryString, Cookie, AcceptLanguageHeader]——
        // Cookie 先于 Accept-Language，满足"手动切换覆盖浏览器检测"，无需自定义。
    }
}
```

- [ ] **Step 5: 创建两个 `.resx` 空壳（各含 1 个探针 key `Probe.Ok`）**

`.resx` 是标准 XML。两个文件均以标准 resheader 头开始，仅 `<data>` 值不同。

`src/WebMail/Resources/SharedResource.en.resx`：
```xml
<?xml version="1.0" encoding="utf-8"?>
<root>
  <resheader name="resmimetype"><value>text/microsoft-resx</value></resheader>
  <resheader name="version"><value>2.0</value></resheader>
  <resheader name="reader"><value>System.Resources.ResXResourceReader, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value></resheader>
  <resheader name="writer"><value>System.Resources.ResXResourceWriter, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value></resheader>
  <data name="Probe.Ok" xml:space="preserve"><value>OK</value></data>
</root>
```

`src/WebMail/Resources/SharedResource.zh-CN.resx`：与上面**完全相同的头**，仅最后一条改为：
```xml
  <data name="Probe.Ok" xml:space="preserve"><value>好</value></data>
```

- [ ] **Step 6: 改 `Program.cs` 接线本地化**

在 `using` 区添加：
```csharp
using WebMail.Services.Localization;
```

将 `builder.Services.AddRazorPages();` 替换为：
```csharp
builder.Services.AddLocalization(options => options.ResourcesPath = "Resources");
builder.Services.AddRazorPages()
    .AddViewLocalization()
    .AddDataAnnotationsLocalization(options =>
        options.DataAnnotationLocalizerProvider = (_, factory) => factory.Create(typeof(SharedResource)));
```

在 `app.UseStaticFiles();` 之后、`app.UseRouting();` 之前插入：
```csharp
app.UseRequestLocalization(LocalizationConfig.Build());
```

- [ ] **Step 7: 改 `_ViewImports.cshtml` 全局注入 `L`**

在文件末尾追加：
```cshtml
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<WebMail.SharedResource> L
```

- [ ] **Step 8: 创建 `TestLocalizer.cs`（回显 key 的桩）**

```csharp
using Microsoft.Extensions.Localization;
using WebMail;

namespace WebMail.Tests;

/// <summary>测试用本地化桩：按 key 回显，使断言与翻译文本解耦。</summary>
public static class TestLocalizer
{
    public static IStringLocalizer<SharedResource> Shared { get; } = new EchoLocalizer();

    private sealed class EchoLocalizer : IStringLocalizer<SharedResource>
    {
        public LocalizedString this[string name] => new(name, name, resourceNotFound: false);
        public LocalizedString this[string name, params object[] arguments] =>
            new(name, arguments.Length == 0 ? name : $"{name}|{string.Join(",", arguments)}", resourceNotFound: false);
        public IEnumerable<LocalizedString> GetAllStrings(bool includeParentCultures) =>
            Enumerable.Empty<LocalizedString>();
    }
}
```

- [ ] **Step 9: 运行测试确认通过 + 全量构建**

Run: `dotnet build WebMail.sln`
Expected: 构建成功（resx 编译为附属程序集）。
Run: `dotnet test tests/WebMail.Tests --filter LocalizationConfigTests`
Expected: PASS。

- [ ] **Step 10: 提交**

```bash
git add src/WebMail/SharedResource.cs src/WebMail/Services/Localization/LocalizationConfig.cs \
        src/WebMail/Resources src/WebMail/Program.cs src/WebMail/Pages/_ViewImports.cshtml \
        tests/WebMail.Tests/TestLocalizer.cs tests/WebMail.Tests/LocalizationConfigTests.cs
git commit -m "feat(i18n): add localization infrastructure (shared resource, request localization, view inject)"
```

---

## Task 2: 资源词条目录（填充全部 key）

**Files:**
- Modify: `src/WebMail/Resources/SharedResource.en.resx`
- Modify: `src/WebMail/Resources/SharedResource.zh-CN.resx`
- Test: `tests/WebMail.Tests/SharedResourceParityTests.cs`

**Interfaces:**
- Produces: 两个 `.resx` 含下表**全部 key**（删除探针 `Probe.Ok`）。后续 Task 4–9 只引用这些 key，不再新增。

**完整词条目录**（每条按 `.resx` 的 `<data name="KEY" xml:space="preserve"><value>...</value></data>` 格式写入；两文件 key 必须一致，仅 `<value>` 不同）：

| key | en | zh-CN |
|---|---|---|
| `Brand.Name` | WebMail | WebMail |
| `Nav.Home` | Home | 首页 |
| `Nav.AdminConsole` | Admin Console | 管理后台 |
| `Nav.Admin.Sales` | Sales Management | 销售员管理 |
| `Nav.Admin.Suppliers` | Supplier Management | 供应商管理 |
| `Nav.Admin.Buyers` | Buyer Management | 买家管理 |
| `Nav.SalesConsole` | Sales Console | 销售后台 |
| `Nav.SupplierConsole` | Supplier Console | 供应商后台 |
| `Nav.Login` | Sign in | 登录 |
| `Nav.Logout` | Sign out | 退出 |
| `Lang.Label` | Language | 语言 |
| `Lang.English` | English | English |
| `Lang.Chinese` | 中文 | 中文 |
| `Footer.Privacy` | Privacy | 隐私 |
| `Privacy.Title` | Privacy Policy | 隐私政策 |
| `Privacy.Body` | Use this page to detail your site's privacy policy. | 在此页面说明本站的隐私政策。 |
| `Login.Title` | Sign in | 登录 |
| `Login.Heading` | Console Sign-in | 后台登录 |
| `Login.UserName` | Username | 用户名 |
| `Login.Password` | Password | 密码 |
| `Login.Submit` | Sign in | 登录 |
| `Login.InvalidCredentials` | Incorrect username or password | 用户名或密码错误 |
| `Login.AccountDisabled` | Account is disabled | 账号已被禁用 |
| `AccessDenied.Title` | Access denied | 无权访问 |
| `AccessDenied.Body` | Your account does not have permission to view this page. | 当前账号没有访问该页面的权限。 |
| `AccessDenied.BackHome` | Back to home | 返回首页 |
| `RoleNoun.Sales` | Sales | 销售员 |
| `RoleNoun.Supplier` | Supplier | 供应商 |
| `Common.Delete` | Delete | 删除 |
| `Common.Reset` | Reset | 重置 |
| `Common.Filter` | Filter | 筛选 |
| `Common.Create` | Create | 创建 |
| `Common.NoBuyers` | No buyers yet. | 暂无买家。 |
| `Common.BackToList` | Back to list | 返回列表 |
| `Common.NoMail` | No mail yet. | 暂无邮件。 |
| `Table.CardNo` | Card key | 卡密 |
| `Table.EmailStatus` | Email status | 邮箱状态 |
| `Table.BuyerStatus` | Buyer status | 买家状态 |
| `Table.SupplierStatus` | Supplier status | 供应商状态 |
| `Table.SupplierProcessingStatus` | Supplier processing status | 供应商处理状态 |
| `Table.CreatedAt` | Created at | 创建时间 |
| `Table.Operations` | Actions | 操作 |
| `Table.UserName` | Username | 用户名 |
| `Table.DisplayName` | Display name | 显示名 |
| `Table.Status` | Status | 状态 |
| `Table.LinkedBuyers` | Linked buyers | 关联买家数 |
| `Table.Source` | Source | 来源 |
| `Table.Sender` | Sender | 发件人 |
| `Table.Subject` | Subject | 主题 |
| `Table.SentAt` | Sent at | 发送时间 |
| `Filter.AllBuyerStatus` | All buyer statuses | 全部买家状态 |
| `Filter.AllEmailStatus` | All email statuses | 全部邮箱状态 |
| `Filter.CardKeyword` | Card key keyword | 卡密关键字 |
| `Action.Approve` | Approve | 通过 |
| `Action.Reject` | Reject | 拒绝 |
| `Action.ResetPassword` | Reset password | 重置密码 |
| `Action.Enable` | Enable | 启用 |
| `Action.Disable` | Disable | 禁用 |
| `Action.ViewMail` | View mail | 查看邮件 |
| `Action.MarkFailed` | Mark failed | 标记失败 |
| `Action.MarkCompleted` | Mark completed | 标记完成 |
| `Action.AuthorizeGmail` | Authorize Gmail | 授权 Gmail |
| `Action.AuthorizeOutlook` | Authorize Outlook | 授权 Outlook |
| `Action.ReAuthorize` | Re-authorize | 重新授权 |
| `Action.ChangeEmail` | Change email | 更改邮箱 |
| `Action.ClearAuth` | Clear authorization | 清空授权 |
| `Status.Enabled` | Enabled | 启用 |
| `Status.Disabled` | Disabled | 禁用 |
| `Field.UserName` | Username | 用户名 |
| `Field.DisplayName` | Display name | 显示名 |
| `Field.InitialPassword` | Initial password (≥6 chars) | 初始密码（≥6位） |
| `Field.NewPassword` | New password | 新密码 |
| `Admin.Buyers.Title` | Buyers (Admin) | 买家列表（管理员） |
| `Admin.Buyers.Deleted` | Buyer deleted. | 已删除买家。 |
| `Admin.Buyers.DeleteFailed` | Cannot delete this buyer. | 无法删除该买家。 |
| `Admin.Buyers.Reviewed` | Review status updated. | 已更新审核状态。 |
| `Admin.Buyers.ReviewFailed` | Cannot review this buyer. | 无法审核该买家。 |
| `Admin.Users.Title` | Manage {0} | {0}管理 |
| `Admin.Users.CreateHeading` | Add {0} | 新建{0} |
| `Admin.Users.Empty` | No {0} yet. | 暂无{0}。 |
| `User.UserNameRequired` | Username cannot be empty. | 用户名不能为空。 |
| `User.PasswordTooShort` | Password must be at least 6 characters. | 密码至少需要 6 位。 |
| `User.UserNameExists` | Username already exists. | 用户名已存在。 |
| `User.Created` | Account created. | 已创建账号。 |
| `User.PasswordReset` | Password reset. | 已重置密码。 |
| `User.NotFound` | Account not found. | 账号不存在。 |
| `User.Enabled` | Account enabled. | 已启用账号。 |
| `User.Disabled` | Account disabled. | 已禁用账号。 |
| `Sales.Buyers.Title` | My buyers (Sales) | 我的买家（销售） |
| `Sales.Buyers.NotFound` | Buyer not found. | 买家不存在。 |
| `Sales.Buyers.Deleted` | Buyer deleted. | 已删除买家。 |
| `Sales.Buyers.DeleteFailed` | Cannot delete this buyer. | 无法删除该买家。 |
| `Supplier.Buyers.Title` | Buyers (Supplier) | 买家列表（供应商） |
| `Supplier.Tab.Unprocessed` | Unprocessed | 未处理 |
| `Supplier.Tab.Failed` | Failed | 失败 |
| `Supplier.Tab.Completed` | Completed | 完成 |
| `Supplier.Status.Unprocessed` | Unprocessed | 未处理 |
| `Supplier.Status.Failed` | Failed | 失败 |
| `Supplier.Status.Completed` | Completed | 完成 |
| `Supplier.InvalidStatus` | Invalid status. | 无效的状态。 |
| `Supplier.StatusUpdated` | Processing status updated. | 已更新处理状态。 |
| `Supplier.StatusUpdateFailed` | Cannot update this buyer's processing status. | 无法更新该买家的处理状态。 |
| `Supplier.Mail.Title` | Buyer mail | 买家邮件 |
| `Supplier.Mail.WindowExpires` | Sync window expires at: | 同步窗口到期时间： |
| `Mail.Junk` | Junk | 垃圾邮件 |
| `Buyer.Verify.Title` | Verify card key | 验证卡密 |
| `Buyer.LinkInvalid` | Invalid link | 链接无效 |
| `Buyer.LinkInvalidOrExpired` | Link is invalid or expired | 链接无效或已失效 |
| `Buyer.UnlinkBlocked` | Processing in progress; cannot remove | 正在处理中，无法删除 |
| `Buyer.Email.Title` | My mailbox | 我的邮箱 |
| `Buyer.Email.CardStatus` | Card status | 卡密状态 |
| `Buyer.Email.BoundEmail` | Linked email: | 已绑定邮箱： |
| `Buyer.Email.NoActions` | No actions available for the current status. | 当前状态暂无可执行的操作。 |
| `OAuth.Callback.Title` | Email authorization | 邮箱授权 |
| `OAuth.Success` | Authorization succeeded; awaiting admin review. | 授权成功，等待管理员审核。 |
| `OAuth.BackToStatus` | Back to mailbox status | 返回邮箱状态 |
| `OAuth.AuthorizationFailed` | Authorization failed: {0} | 授权失败：{0} |
| `OAuth.CallbackInvalid` | Invalid authorization callback parameters | 授权回调参数无效 |

- [ ] **Step 1: 写失败测试 `SharedResourceParityTests`**

```csharp
using System.Globalization;
using System.Resources;
using WebMail;
using Xunit;

namespace WebMail.Tests;

public sealed class SharedResourceParityTests
{
    private static HashSet<string> KeysFor(string culture)
    {
        var rm = new ResourceManager("WebMail.Resources.SharedResource", typeof(SharedResource).Assembly);
        using var set = rm.GetResourceSet(new CultureInfo(culture), createIfNotExists: true, tryParents: false)
            ?? throw new Xunit.Sdk.XunitException($"No resource set for {culture}");
        return set.Cast<System.Collections.DictionaryEntry>().Select(e => (string)e.Key).ToHashSet();
    }

    [Fact]
    public void EnglishAndChineseHaveIdenticalKeys()
    {
        var en = KeysFor("en");
        var zh = KeysFor("zh-CN");
        Assert.Empty(en.Except(zh));   // 中文缺失的 key
        Assert.Empty(zh.Except(en));   // 英文缺失的 key
        Assert.True(en.Count > 50, $"Expected full catalog, got {en.Count}");
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/WebMail.Tests --filter SharedResourceParityTests`
Expected: FAIL（当前仅探针 key，数量 < 50 且无业务 key）。

- [ ] **Step 3: 填充两个 `.resx`**

删除 `Probe.Ok`，按上表为每个 key 在两文件中各加一条 `<data>`。两文件 key 顺序保持一致以便 diff。`zh-CN` 文件的 `<value>` 取"zh-CN"列，`en` 文件取"en"列。

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/WebMail.Tests --filter SharedResourceParityTests`
Expected: PASS。
Run: `dotnet build WebMail.sln`
Expected: 成功。

- [ ] **Step 5: 提交**

```bash
git add src/WebMail/Resources tests/WebMail.Tests/SharedResourceParityTests.cs
git commit -m "feat(i18n): populate shared resource catalog (en/zh-CN) with parity test"
```

---

## Task 3: 语言切换器页面

**Files:**
- Create: `src/WebMail/Pages/Culture/Set.cshtml`
- Create: `src/WebMail/Pages/Culture/Set.cshtml.cs`
- Test: `tests/WebMail.Tests/CultureSetModelTests.cs`

**Interfaces:**
- Consumes: `WebMail.Services.Auth.AuthRouting.IsLocalUrl(string?)`（既有，用于防开放重定向）；`WebMail.Services.Localization.LocalizationConfig.SupportedCultureNames`（Task 1）。
- Produces: `namespace WebMail.Pages.Culture; public sealed class SetModel : PageModel`，含 `public IActionResult OnGet(string? culture, string? returnUrl)`。

- [ ] **Step 1: 写失败测试 `CultureSetModelTests`**

```csharp
using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Localization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;
using WebMail.Pages.Culture;
using Xunit;

namespace WebMail.Tests;

public sealed class CultureSetModelTests
{
    private static SetModel CreateModel(out HttpContext ctx)
    {
        ctx = new DefaultHttpContext();
        return new SetModel { PageContext = new PageContext { HttpContext = ctx } };
    }

    [Fact]
    public void SupportedCulture_WritesCookie_AndRedirectsToLocalReturnUrl()
    {
        var model = CreateModel(out var ctx);

        var result = model.OnGet("zh-CN", "/Admin/Buyers");

        Assert.Equal("/Admin/Buyers", Assert.IsType<RedirectResult>(result).Url);
        var setCookie = ctx.Response.Headers["Set-Cookie"].ToString();
        Assert.Contains(CookieRequestCultureProvider.DefaultCookieName, setCookie);
        Assert.Contains("zh-CN", setCookie);
    }

    [Fact]
    public void NonLocalReturnUrl_RedirectsToRoot()
    {
        var model = CreateModel(out _);

        var result = model.OnGet("en", "https://evil.com");

        Assert.Equal("/", Assert.IsType<RedirectResult>(result).Url);
    }

    [Fact]
    public void UnsupportedCulture_DoesNotWriteCookie()
    {
        var model = CreateModel(out var ctx);

        var result = model.OnGet("fr-FR", "/");

        Assert.Equal("/", Assert.IsType<RedirectResult>(result).Url);
        Assert.True(string.IsNullOrEmpty(ctx.Response.Headers["Set-Cookie"].ToString()));
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/WebMail.Tests --filter CultureSetModelTests`
Expected: 编译失败（`SetModel` 不存在）。

- [ ] **Step 3: 创建 `Set.cshtml.cs`**

```csharp
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Localization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;
using WebMail.Services.Auth;
using WebMail.Services.Localization;

namespace WebMail.Pages.Culture;

[AllowAnonymous]
public sealed class SetModel : PageModel
{
    public IActionResult OnGet(string? culture, string? returnUrl)
    {
        var target = AuthRouting.IsLocalUrl(returnUrl) ? returnUrl! : "/";

        if (!string.IsNullOrEmpty(culture) && LocalizationConfig.SupportedCultureNames.Contains(culture))
        {
            Response.Cookies.Append(
                CookieRequestCultureProvider.DefaultCookieName,
                CookieRequestCultureProvider.MakeCookieValue(new RequestCulture(culture)),
                new CookieOptions
                {
                    Expires = DateTimeOffset.UtcNow.AddYears(1),
                    IsEssential = true,
                    Path = "/",
                });
        }

        return Redirect(target);
    }
}
```

- [ ] **Step 4: 创建 `Set.cshtml`（仅路由，无正文）**

```cshtml
@page
@model WebMail.Pages.Culture.SetModel
```

- [ ] **Step 5: 运行确认通过 + 构建**

Run: `dotnet test tests/WebMail.Tests --filter CultureSetModelTests`
Expected: PASS（3 个）。
Run: `dotnet build WebMail.sln`
Expected: 成功。

- [ ] **Step 6: 提交**

```bash
git add src/WebMail/Pages/Culture tests/WebMail.Tests/CultureSetModelTests.cs
git commit -m "feat(i18n): add /Culture/Set language switcher endpoint"
```

---

## Task 4: 布局——切换器 UI + 动态 lang + 导航文字

**Files:**
- Modify: `src/WebMail/Pages/Shared/_Layout.cshtml`

**Interfaces:**
- Consumes: 全局 `L`（Task 1）；keys `Nav.*`、`Lang.*`、`Footer.Privacy`、`Brand.Name`（Task 2）。

- [ ] **Step 1: 改 `_Layout.cshtml`**

将 `<html lang="en">` 改为：
```cshtml
<html lang="@System.Globalization.CultureInfo.CurrentUICulture.Name">
```

替换导航文字（保持原有 `asp-*` 属性与结构，仅替换可见文本）：
- 品牌：`WebMail` → `@L["Brand.Name"]`
- `>Home<` → `>@L["Nav.Home"]<`
- `管理后台` → `@L["Nav.AdminConsole"]`
- `销售员管理` → `@L["Nav.Admin.Sales"]`
- `供应商管理` → `@L["Nav.Admin.Suppliers"]`
- `买家管理` → `@L["Nav.Admin.Buyers"]`
- `销售后台` → `@L["Nav.SalesConsole"]`
- `供应商后台` → `@L["Nav.SupplierConsole"]`
- `退出` → `@L["Nav.Logout"]`
- `登录` → `@L["Nav.Login"]`
- 页脚 `Privacy` → `@L["Footer.Privacy"]`

在右侧 `<ul class="navbar-nav">`（用户/登录所在的那个 `ul`）**之前**插入语言切换下拉。先在 `_Layout.cshtml` 顶部（`<!DOCTYPE html>` 之上）加入当前路径计算：
```cshtml
@{
    var returnUrl = $"{Context.Request.Path}{Context.Request.QueryString}";
    var currentCulture = System.Globalization.CultureInfo.CurrentUICulture.Name;
}
```
切换下拉（放在右侧 `ul` 内、用户名条目之前）：
```cshtml
<li class="nav-item dropdown">
    <a class="nav-link text-dark dropdown-toggle" href="#" role="button"
       data-bs-toggle="dropdown" aria-expanded="false">@L["Lang.Label"]</a>
    <ul class="dropdown-menu dropdown-menu-end">
        <li>
            <a class="dropdown-item @(currentCulture == "zh-CN" ? "active" : "")"
               asp-page="/Culture/Set" asp-route-culture="zh-CN" asp-route-returnUrl="@returnUrl">@L["Lang.Chinese"]</a>
        </li>
        <li>
            <a class="dropdown-item @(currentCulture == "en" ? "active" : "")"
               asp-page="/Culture/Set" asp-route-culture="en" asp-route-returnUrl="@returnUrl">@L["Lang.English"]</a>
        </li>
    </ul>
</li>
```

- [ ] **Step 2: 构建并启动手动验证**

Run: `dotnet build WebMail.sln`
Expected: 成功。
手动：`dotnet run --project src/WebMail`，打开站点，确认导航出现"语言/Language"下拉，切换中/英后导航文字与 `<html lang>` 随之变化，刷新后保持（Cookie 生效）。

- [ ] **Step 3: 运行测试确保无回归**

Run: `dotnet test tests/WebMail.Tests`
Expected: 全绿。

- [ ] **Step 4: 提交**

```bash
git add src/WebMail/Pages/Shared/_Layout.cshtml
git commit -m "feat(i18n): localize layout nav and add language switcher dropdown"
```

---

## Task 5: 登录 / 拒绝访问 / 隐私 页面

**Files:**
- Modify: `src/WebMail/Pages/Login.cshtml`
- Modify: `src/WebMail/Pages/Login.cshtml.cs`
- Modify: `src/WebMail/Pages/AccessDenied.cshtml`
- Modify: `src/WebMail/Pages/Privacy.cshtml`
- Modify: `tests/WebMail.Tests/LoginModelTests.cs`

**Interfaces:**
- Consumes: 全局 `L`；keys `Login.*`、`AccessDenied.*`、`Privacy.*`。
- Produces: `LoginModel` 构造函数签名变为 `LoginModel(WebMailDbContext db, IPasswordHasher<AppUser> hasher, IStringLocalizer<SharedResource> loc)`。

- [ ] **Step 1: 改 `Login.cshtml.cs`——注入 localizer，消息改 key**

在 `using` 加：
```csharp
using Microsoft.Extensions.Localization;
using WebMail;
```
改字段与构造函数：
```csharp
    private readonly WebMailDbContext _db;
    private readonly IPasswordHasher<AppUser> _hasher;
    private readonly IStringLocalizer<SharedResource> _loc;

    public LoginModel(WebMailDbContext db, IPasswordHasher<AppUser> hasher, IStringLocalizer<SharedResource> loc)
    {
        _db = db;
        _hasher = hasher;
        _loc = loc;
    }
```
两处消息：
- `ErrorMessage = "用户名或密码错误";` → `ErrorMessage = _loc["Login.InvalidCredentials"];`
- `ErrorMessage = "账号已被禁用";` → `ErrorMessage = _loc["Login.AccountDisabled"];`

- [ ] **Step 2: 改 `Login.cshtml`**

- `ViewData["Title"] = "登录";` → `ViewData["Title"] = L["Login.Title"].Value;`
- `后台登录` → `@L["Login.Heading"]`
- label `用户名` → `@L["Login.UserName"]`
- label `密码` → `@L["Login.Password"]`
- 按钮 `登录` → `@L["Login.Submit"]`
（`@Model.ErrorMessage` 已是本地化文本，原样渲染。）

- [ ] **Step 3: 改 `AccessDenied.cshtml`**

- `ViewData["Title"] = "无权访问";` → `= L["AccessDenied.Title"].Value;`
- `<h1 class="h3">无权访问</h1>` → `<h1 class="h3">@L["AccessDenied.Title"]</h1>`
- `当前账号没有访问该页面的权限。` → `@L["AccessDenied.Body"]`
- `返回首页` → `@L["AccessDenied.BackHome"]`

- [ ] **Step 4: 改 `Privacy.cshtml`**

- `ViewData["Title"] = "Privacy Policy";` → `= L["Privacy.Title"].Value;`
- `<p>Use this page...</p>` → `<p>@L["Privacy.Body"]</p>`
（`<h1>@ViewData["Title"]</h1>` 不变。）

- [ ] **Step 5: 改 `LoginModelTests.cs`——断言改 key，构造补 localizer**

将 3 处 `new LoginModel(db, hasher)` 改为 `new LoginModel(db, hasher, TestLocalizer.Shared)`（共 7 个构造点，全部补第三参数）。
断言改为 key：
- 两处 `Assert.Equal("用户名或密码错误", model.ErrorMessage);` → `Assert.Equal("Login.InvalidCredentials", model.ErrorMessage);`
- 一处 `Assert.Equal("账号已被禁用", model.ErrorMessage);` → `Assert.Equal("Login.AccountDisabled", model.ErrorMessage);`
在文件顶部确保 `using Microsoft.Extensions.Localization;` 不需要（TestLocalizer 已封装）。

- [ ] **Step 6: 构建并测试**

Run: `dotnet build WebMail.sln`
Run: `dotnet test tests/WebMail.Tests --filter LoginModelTests`
Expected: PASS。
Run: `dotnet test tests/WebMail.Tests`
Expected: 全绿。

- [ ] **Step 7: 提交**

```bash
git add src/WebMail/Pages/Login.cshtml src/WebMail/Pages/Login.cshtml.cs \
        src/WebMail/Pages/AccessDenied.cshtml src/WebMail/Pages/Privacy.cshtml \
        tests/WebMail.Tests/LoginModelTests.cs
git commit -m "feat(i18n): localize login, access-denied, privacy pages"
```

---

## Task 6: 管理后台（用户管理 + 买家管理）

**Files:**
- Modify: `src/WebMail/Services/UserAdminService.cs`
- Modify: `src/WebMail/Pages/Admin/Sales.cshtml.cs`
- Modify: `src/WebMail/Pages/Admin/Suppliers.cshtml.cs`
- Modify: `src/WebMail/Pages/Admin/_UserManagement.cshtml`
- Modify: `src/WebMail/Pages/Admin/Sales.cshtml`
- Modify: `src/WebMail/Pages/Admin/Suppliers.cshtml`
- Modify: `src/WebMail/Pages/Admin/Buyers.cshtml`
- Modify: `src/WebMail/Pages/Admin/Buyers.cshtml.cs`
- Modify: `tests/WebMail.Tests/AdminSalesModelTests.cs`
- Modify: `tests/WebMail.Tests/AdminSuppliersModelTests.cs`
- Modify: `tests/WebMail.Tests/AdminBuyersModelTests.cs`

**Interfaces:**
- Produces:
  - `UserAdminService` 的 `UserAdminResult.Message` 字段值改为 **key 字符串**（服务不注入 localizer）。
  - `SalesModel` 构造函数新增 `IStringLocalizer<SharedResource> loc`；移除返回中文的 `RoleTitle`，新增 `public string RoleNounKey => Role == UserRole.Supplier ? "RoleNoun.Supplier" : "RoleNoun.Sales";`；`Message` 在 handler 中本地化。
  - `SuppliersModel` 构造函数透传 localizer。
  - Admin `BuyersModel` 构造函数新增 `IStringLocalizer<SharedResource> loc`；消息改本地化 key。

- [ ] **Step 1: 改 `UserAdminService.cs`——消息字面量改 key**

逐条替换 `return new(bool, "中文")` 的消息为 key（其余逻辑不动）：
- `"用户名不能为空。"` → `"User.UserNameRequired"`
- 两处 `$"密码至少需要 {MinPasswordLength} 位。"` → `"User.PasswordTooShort"`
- `"用户名已存在。"` → `"User.UserNameExists"`
- `"已创建账号。"` → `"User.Created"`
- `"已重置密码。"` → `"User.PasswordReset"`
- 两处 `"账号不存在。"` → `"User.NotFound"`
- `isActive ? "已启用账号。" : "已禁用账号。"` → `isActive ? "User.Enabled" : "User.Disabled"`

- [ ] **Step 2: 改 `Sales.cshtml.cs`（SalesModel）——注入 localizer，本地化 Message，替换 RoleTitle**

`using` 加 `using Microsoft.Extensions.Localization;` 和 `using WebMail;`。
改构造：
```csharp
    private readonly UserAdminService _users;
    private readonly IStringLocalizer<SharedResource> _loc;

    public SalesModel(UserAdminService users, IStringLocalizer<SharedResource> loc)
    {
        _users = users;
        _loc = loc;
    }
```
替换 `RoleTitle`：
```csharp
    public string RoleNounKey => Role == UserRole.Supplier ? "RoleNoun.Supplier" : "RoleNoun.Sales";
```
（删除 `public string RoleTitle => ...;`）
三个 handler 把 `Message = (await ...).Message;` 改为本地化：
```csharp
        Message = _loc[(await _users.CreateAsync(Role, NewUserName, NewDisplayName, NewPassword, AdminId())).Message];
```
同理 `OnPostResetPasswordAsync`、`OnPostSetActiveAsync` 用 `_loc[(...).Message]`。

- [ ] **Step 3: 改 `Suppliers.cshtml.cs`（SuppliersModel）——透传 localizer**

```csharp
    public SuppliersModel(UserAdminService users, IStringLocalizer<SharedResource> loc) : base(users, loc)
    {
    }
```
`using` 补 `using Microsoft.Extensions.Localization;` 与 `using WebMail;`。

- [ ] **Step 4: 改 `_UserManagement.cshtml`**

- `<h1 class="display-6">@(Model.RoleTitle)管理</h1>` → `<h1 class="display-6">@L["Admin.Users.Title", L[Model.RoleNounKey].Value]</h1>`
- `<h2 class="h5">新建@(Model.RoleTitle)</h2>` → `<h2 class="h5">@L["Admin.Users.CreateHeading", L[Model.RoleNounKey].Value]</h2>`
- placeholder `用户名` → `placeholder="@L["Field.UserName"]"`
- placeholder `显示名` → `placeholder="@L["Field.DisplayName"]"`
- placeholder `初始密码（≥6位）` → `placeholder="@L["Field.InitialPassword"]"`
- 按钮 `创建` → `@L["Common.Create"]`
- `<p>暂无@(Model.RoleTitle)。</p>` → `<p>@L["Admin.Users.Empty", L[Model.RoleNounKey].Value]</p>`
- 表头：`用户名`→`@L["Table.UserName"]`、`显示名`→`@L["Table.DisplayName"]`、`状态`→`@L["Table.Status"]`、`关联买家数`→`@L["Table.LinkedBuyers"]`、`创建时间`→`@L["Table.CreatedAt"]`、`操作`→`@L["Table.Operations"]`
- `@(u.IsActive ? "启用" : "禁用")` → `@(u.IsActive ? L["Status.Enabled"].Value : L["Status.Disabled"].Value)`
- placeholder `新密码` → `placeholder="@L["Field.NewPassword"]"`
- 按钮 `重置密码` → `@L["Action.ResetPassword"]`
- `@(u.IsActive ? "禁用" : "启用")` → `@(u.IsActive ? L["Action.Disable"].Value : L["Action.Enable"].Value)`

注意：`_UserManagement.cshtml` 顶部 `@model WebMail.Pages.Admin.SalesModel` 不变（`L` 来自 `_ViewImports`）。

- [ ] **Step 5: 改 `Admin/Sales.cshtml` 与 `Admin/Suppliers.cshtml` 标题**

两文件均把：
```cshtml
    ViewData["Title"] = $"{Model.RoleTitle}管理";
```
改为：
```cshtml
    ViewData["Title"] = L["Admin.Users.Title", L[Model.RoleNounKey].Value].Value;
```

- [ ] **Step 6: 改 `Admin/Buyers.cshtml`**

- `ViewData["Title"] = "买家列表（管理员）";` → `= L["Admin.Buyers.Title"].Value;`
- `<h1 class="display-6">买家列表（管理员）</h1>` → `@L["Admin.Buyers.Title"]`
- `<option value="">全部买家状态</option>` → `@L["Filter.AllBuyerStatus"]`
- `<option value="">全部邮箱状态</option>` → `@L["Filter.AllEmailStatus"]`
- placeholder `卡密关键字` → `placeholder="@L["Filter.CardKeyword"]"`
- 按钮 `筛选` → `@L["Common.Filter"]`
- 链接 `重置` → `@L["Common.Reset"]`
- `<p>暂无买家。</p>` → `@L["Common.NoBuyers"]`
- 表头：`卡密`→`@L["Table.CardNo"]`、`邮箱状态`→`@L["Table.EmailStatus"]`、`买家状态`→`@L["Table.BuyerStatus"]`、`供应商状态`→`@L["Table.SupplierStatus"]`、`创建时间`→`@L["Table.CreatedAt"]`、`操作`→`@L["Table.Operations"]`
- 按钮 `通过`→`@L["Action.Approve"]`、`拒绝`→`@L["Action.Reject"]`、`删除`→`@L["Common.Delete"]`

- [ ] **Step 7: 改 `Admin/Buyers.cshtml.cs`——注入 localizer + 消息 key**

`using` 加 `using Microsoft.Extensions.Localization;`、`using WebMail;`。改构造：
```csharp
    private readonly WebMailDbContext _db;
    private readonly IStringLocalizer<SharedResource> _loc;

    public BuyersModel(WebMailDbContext db, IStringLocalizer<SharedResource> loc)
    {
        _db = db;
        _loc = loc;
    }
```
消息：
- `Message = "已删除买家。";` → `Message = _loc["Admin.Buyers.Deleted"];`
- `Message = "无法删除该买家。";` → `Message = _loc["Admin.Buyers.DeleteFailed"];`
- `Message = "已更新审核状态。";` → `Message = _loc["Admin.Buyers.Reviewed"];`
- `Message = "无法审核该买家。";` → `Message = _loc["Admin.Buyers.ReviewFailed"];`

- [ ] **Step 8: 更新管理后台测试构造点**

- `AdminSalesModelTests.cs`：所有 `new SalesModel(<users>)` → `new SalesModel(<users>, TestLocalizer.Shared)`。
- `AdminSuppliersModelTests.cs`：所有 `new SuppliersModel(<users>)` → `new SuppliersModel(<users>, TestLocalizer.Shared)`。
- `AdminBuyersModelTests.cs`：所有 `new BuyersModel(<db>)` → `new BuyersModel(<db>, TestLocalizer.Shared)`。

（这些测试未断言 `Message`/`RoleTitle` 文本——确认无文本断言；若有断言 `RoleTitle` 或中文 Message，改为断言对应 key。）

- [ ] **Step 9: 构建并测试**

Run: `dotnet build WebMail.sln`
Run: `dotnet test tests/WebMail.Tests`
Expected: 全绿。
手动（可选）：登录管理员，切换中/英，确认用户管理标题"管理销售员/Manage Sales"、表头、按钮、操作消息均随语言变化。

- [ ] **Step 10: 提交**

```bash
git add src/WebMail/Services/UserAdminService.cs src/WebMail/Pages/Admin tests/WebMail.Tests/AdminSalesModelTests.cs tests/WebMail.Tests/AdminSuppliersModelTests.cs tests/WebMail.Tests/AdminBuyersModelTests.cs
git commit -m "feat(i18n): localize admin user-management and buyer-management pages"
```

---

## Task 7: 销售后台（我的买家）

**Files:**
- Modify: `src/WebMail/Pages/Sales/Buyers.cshtml`
- Modify: `src/WebMail/Pages/Sales/Buyers.cshtml.cs`
- Modify: `tests/WebMail.Tests/*`（仅当存在构造 `Sales.BuyersModel` 的测试时）

**Interfaces:**
- Produces: `Sales.BuyersModel` 构造函数新增 `IStringLocalizer<SharedResource> loc`；消息改 key。

- [ ] **Step 1: 改 `Sales/Buyers.cshtml.cs`——注入 localizer + 消息**

`using` 加 `using Microsoft.Extensions.Localization;`、`using WebMail;`。改构造：
```csharp
    private readonly WebMailDbContext _db;
    private readonly BuyerRuleService _ruleService;
    private readonly IStringLocalizer<SharedResource> _loc;

    public BuyersModel(WebMailDbContext db, BuyerRuleService ruleService, IStringLocalizer<SharedResource> loc)
    {
        _db = db;
        _ruleService = ruleService;
        _loc = loc;
    }
```
消息：
- `Message = "买家不存在。";` → `Message = _loc["Sales.Buyers.NotFound"];`
- `Message = "已删除买家。";` → `Message = _loc["Sales.Buyers.Deleted"];`
- `Message = "无法删除该买家。";` → `Message = _loc["Sales.Buyers.DeleteFailed"];`

- [ ] **Step 2: 改 `Sales/Buyers.cshtml`**

- `ViewData["Title"] = "我的买家（销售）";` → `= L["Sales.Buyers.Title"].Value;`
- `<h1 class="display-6">我的买家（销售）</h1>` → `@L["Sales.Buyers.Title"]`
- `<p>暂无买家。</p>` → `@L["Common.NoBuyers"]`
- 表头：`卡密`→`@L["Table.CardNo"]`、`邮箱状态`→`@L["Table.EmailStatus"]`、`买家状态`→`@L["Table.BuyerStatus"]`、`供应商处理状态`→`@L["Table.SupplierProcessingStatus"]`、`创建时间`→`@L["Table.CreatedAt"]`、`操作`→`@L["Table.Operations"]`
- 按钮 `删除` → `@L["Common.Delete"]`

- [ ] **Step 3: 更新销售测试构造点（若有）**

搜索 `new WebMail.Pages.Sales` 或 `Sales.BuyersModel` 的构造；存在则补 `, TestLocalizer.Shared`。运行 `grep -rn "Sales.BuyersModel\|Pages.Sales" tests` 确认。

- [ ] **Step 4: 构建并测试**

Run: `dotnet build WebMail.sln`
Run: `dotnet test tests/WebMail.Tests`
Expected: 全绿。

- [ ] **Step 5: 提交**

```bash
git add src/WebMail/Pages/Sales tests/WebMail.Tests
git commit -m "feat(i18n): localize sales buyers page"
```

---

## Task 8: 供应商后台（列表 / 表格 / 邮件 / 状态徽章）

**Files:**
- Modify: `src/WebMail/Pages/Supplier/Buyers.cshtml`
- Modify: `src/WebMail/Pages/Supplier/Buyers.cshtml.cs`
- Modify: `src/WebMail/Pages/Supplier/_BuyerTable.cshtml`
- Modify: `src/WebMail/Pages/Supplier/Mail.cshtml`
- Modify: `src/WebMail/Pages/Shared/_SupplierStatusBadge.cshtml`
- Modify: `tests/WebMail.Tests/SupplierBuyersModelTests.cs`

**Interfaces:**
- Produces:
  - `Supplier.BuyersModel` 构造函数新增 `IStringLocalizer<SharedResource> loc`；消息改 key。

> **注：** `BuyerRuleService.BuyerUnlinkBlockedMessage` 的改名留到 Task 9 Step 1，连同其全部 3 个调用方（`Email.cshtml.cs` 两处、`Callback.cshtml.cs` 一处）一起改，以保证每个 Task 结束都可编译。本 Task 8 **不改** `BuyerRuleService.cs`，只做 Supplier 视图与 `Supplier/Buyers.cshtml.cs`（后者不依赖该 property）。

- [ ] **Step 1: 改 `Supplier/Buyers.cshtml.cs`——注入 localizer + 消息**

`using` 加 `using Microsoft.Extensions.Localization;`、`using WebMail;`。改构造：
```csharp
    private readonly WebMailDbContext _db;
    private readonly BuyerRuleService _ruleService;
    private readonly IStringLocalizer<SharedResource> _loc;

    public BuyersModel(WebMailDbContext db, BuyerRuleService ruleService, IStringLocalizer<SharedResource> loc)
    {
        _db = db;
        _ruleService = ruleService;
        _loc = loc;
    }
```
消息：
- `Message = "无效的状态。";` → `Message = _loc["Supplier.InvalidStatus"];`
- `Message = "已更新处理状态。";` → `Message = _loc["Supplier.StatusUpdated"];`
- `Message = "无法更新该买家的处理状态。";` → `Message = _loc["Supplier.StatusUpdateFailed"];`

- [ ] **Step 2: 改 `Supplier/Buyers.cshtml`**

- `ViewData["Title"] = "买家列表（供应商）";` → `= L["Supplier.Buyers.Title"].Value;`
- `<h1 class="display-6">买家列表（供应商）</h1>` → `@L["Supplier.Buyers.Title"]`
- Tab 文字（保留计数）：
  - `未处理 (@unprocessed.Count)` → `@L["Supplier.Tab.Unprocessed"] (@unprocessed.Count)`
  - `失败 (@failed.Count)` → `@L["Supplier.Tab.Failed"] (@failed.Count)`
  - `完成 (@completed.Count)` → `@L["Supplier.Tab.Completed"] (@completed.Count)`

- [ ] **Step 3: 改 `Supplier/_BuyerTable.cshtml`**

- `<p>暂无买家。</p>` → `@L["Common.NoBuyers"]`
- 表头：`卡密`→`@L["Table.CardNo"]`、`邮箱状态`→`@L["Table.EmailStatus"]`、`买家状态`→`@L["Table.BuyerStatus"]`、`供应商状态`→`@L["Table.SupplierStatus"]`、`操作`→`@L["Table.Operations"]`
- 链接 `查看邮件` → `@L["Action.ViewMail"]`
- 按钮 `标记失败` → `@L["Action.MarkFailed"]`
- 按钮 `标记完成` → `@L["Action.MarkCompleted"]`

- [ ] **Step 4: 改 `Supplier/Mail.cshtml`**

- `ViewData["Title"] = "买家邮件";` → `= L["Supplier.Mail.Title"].Value;`
- `<h1 class="display-6">买家邮件</h1>` → `@L["Supplier.Mail.Title"]`
- `<strong>同步窗口到期时间：</strong>` → `<strong>@L["Supplier.Mail.WindowExpires"]</strong>`
- 链接 `返回列表` → `@L["Common.BackToList"]`
- `<p>暂无邮件。</p>` → `@L["Common.NoMail"]`
- 表头：`来源`→`@L["Table.Source"]`、`发件人`→`@L["Table.Sender"]`、`主题`→`@L["Table.Subject"]`、`发送时间`→`@L["Table.SentAt"]`
- 徽章 `垃圾邮件` → `@L["Mail.Junk"]`

- [ ] **Step 5: 改 `_SupplierStatusBadge.cshtml`**

- `<span class="badge bg-danger">失败</span>` → `@L["Supplier.Status.Failed"]` 包在 span 内：`<span class="badge bg-danger">@L["Supplier.Status.Failed"]</span>`
- `<span class="badge bg-success">完成</span>` → `<span class="badge bg-success">@L["Supplier.Status.Completed"]</span>`
- `<span>未处理</span>` → `<span>@L["Supplier.Status.Unprocessed"]</span>`

- [ ] **Step 6: 更新 `SupplierBuyersModelTests.cs` 构造点**

所有 `new BuyersModel(<db>, <ruleService>)` → `new BuyersModel(<db>, <ruleService>, TestLocalizer.Shared)`（注意命名空间 `WebMail.Pages.Supplier`）。若有断言中文 `Message`，改为断言对应 key。

- [ ] **Step 7: 构建并测试**

Run: `dotnet build WebMail.sln`
Run: `dotnet test tests/WebMail.Tests`
Expected: 全绿。

- [ ] **Step 8: 提交**

```bash
git add src/WebMail/Pages/Supplier src/WebMail/Pages/Shared/_SupplierStatusBadge.cshtml tests/WebMail.Tests/SupplierBuyersModelTests.cs
git commit -m "feat(i18n): localize supplier pages, buyer table, status badge"
```

---

## Task 9: 买家入口 / 邮箱 / OAuth 回调 + BuyerRuleService 改名

**Files:**
- Modify: `src/WebMail/Services/BuyerRuleService.cs`
- Modify: `src/WebMail/Pages/Buyer/Verify.cshtml`
- Modify: `src/WebMail/Pages/Buyer/Verify.cshtml.cs`
- Modify: `src/WebMail/Pages/Buyer/Email.cshtml`
- Modify: `src/WebMail/Pages/Buyer/Email.cshtml.cs`
- Modify: `src/WebMail/Pages/OAuth/Callback.cshtml`
- Modify: `src/WebMail/Pages/OAuth/Callback.cshtml.cs`
- Modify: `tests/WebMail.Tests/BuyerPageModelTests.cs`
- Modify: `tests/WebMail.Tests/OAuthCallbackModelTests.cs`

**Interfaces:**
- Consumes: `BuyerRuleService.BuyerUnlinkBlockedMessageKey`（本任务创建）。
- Produces: `VerifyModel`、`EmailModel`、`CallbackModel` 构造函数均新增 `IStringLocalizer<SharedResource> loc`；消息改 key/本地化。

- [ ] **Step 1: 改 `BuyerRuleService.cs` 改名 property**

```csharp
    public string BuyerUnlinkBlockedMessageKey => "Buyer.UnlinkBlocked";
```
（替换 `public string BuyerUnlinkBlockedMessage => "正在处理中，无法删除";`）

- [ ] **Step 2: 改 `Verify.cshtml.cs`——注入 localizer + 消息**

`using` 加 `using Microsoft.Extensions.Localization;`、`using WebMail;`。改构造：
```csharp
    private readonly WebMailDbContext _db;
    private readonly IStringLocalizer<SharedResource> _loc;

    public VerifyModel(WebMailDbContext db, IStringLocalizer<SharedResource> loc)
    {
        _db = db;
        _loc = loc;
    }
```
消息：
- `ErrorMessage = "链接无效";` → `ErrorMessage = _loc["Buyer.LinkInvalid"];`
- `ErrorMessage = "链接无效或已失效";` → `ErrorMessage = _loc["Buyer.LinkInvalidOrExpired"];`

- [ ] **Step 3: 改 `Verify.cshtml`**

- `ViewData["Title"] = "验证卡密";` → `= L["Buyer.Verify.Title"].Value;`
- `<h1 class="display-6">验证卡密</h1>` → `@L["Buyer.Verify.Title"]`

- [ ] **Step 4: 改 `Email.cshtml.cs`——注入 localizer + 消息 + 改名调用**

`using` 加 `using Microsoft.Extensions.Localization;`、`using WebMail;`。改构造：
```csharp
    private readonly WebMailDbContext _db;
    private readonly BuyerRuleService _ruleService;
    private readonly IStringLocalizer<SharedResource> _loc;

    public EmailModel(WebMailDbContext db, BuyerRuleService ruleService, IStringLocalizer<SharedResource> loc)
    {
        _db = db;
        _ruleService = ruleService;
        _loc = loc;
    }
```
消息（共 3 处 `链接无效或已失效` + 2 处 unlink）：
- 每处 `ErrorMessage = "链接无效或已失效";` → `ErrorMessage = _loc["Buyer.LinkInvalidOrExpired"];`
- 两处 `ErrorMessage = _ruleService.BuyerUnlinkBlockedMessage;` → `ErrorMessage = _loc[_ruleService.BuyerUnlinkBlockedMessageKey];`

- [ ] **Step 5: 改 `Email.cshtml`**

- `ViewData["Title"] = "我的邮箱";` → `= L["Buyer.Email.Title"].Value;`
- `<h1 class="display-6">我的邮箱</h1>` → `@L["Buyer.Email.Title"]`
- dl 标签：`卡密状态`→`@L["Buyer.Email.CardStatus"]`、`邮箱状态`→`@L["Table.EmailStatus"]`、`买家状态`→`@L["Table.BuyerStatus"]`、`供应商状态`→`@L["Table.SupplierStatus"]`
- `<strong>已绑定邮箱：</strong>` → `<strong>@L["Buyer.Email.BoundEmail"]</strong>`
- 链接/按钮：`授权 Gmail`→`@L["Action.AuthorizeGmail"]`、`授权 Outlook`→`@L["Action.AuthorizeOutlook"]`、`重新授权`→`@L["Action.ReAuthorize"]`、`更改邮箱`→`@L["Action.ChangeEmail"]`、`清空授权`→`@L["Action.ClearAuth"]`
- `当前状态暂无可执行的操作。` → `@L["Buyer.Email.NoActions"]`

- [ ] **Step 6: 改 `Callback.cshtml.cs`——注入 localizer + 消息（含带参）+ 改名调用**

该类用主构造函数 `CallbackModel(WebMailDbContext db, BuyerRuleService ruleService, IEmailProviderResolver providers)`。新增参数：
```csharp
public sealed class CallbackModel(
    WebMailDbContext db,
    BuyerRuleService ruleService,
    IEmailProviderResolver providers,
    IStringLocalizer<SharedResource> loc) : PageModel
```
`using` 加 `using Microsoft.Extensions.Localization;`、`using WebMail;`。
消息：
- `ErrorMessage = $"授权失败：{error}";` → `ErrorMessage = loc["OAuth.AuthorizationFailed", error];`
- `ErrorMessage = "授权回调参数无效";` → `ErrorMessage = loc["OAuth.CallbackInvalid"];`
- `ErrorMessage = "链接无效或已失效";` → `ErrorMessage = loc["Buyer.LinkInvalidOrExpired"];`
- `ErrorMessage = ruleService.BuyerUnlinkBlockedMessage;` → `ErrorMessage = loc[ruleService.BuyerUnlinkBlockedMessageKey];`

- [ ] **Step 7: 改 `Callback.cshtml`**

- `ViewData["Title"] = "邮箱授权";` → `= L["OAuth.Callback.Title"].Value;`
- `<h1 class="display-6">邮箱授权</h1>` → `@L["OAuth.Callback.Title"]`
- `授权成功，等待管理员审核。` → `@L["OAuth.Success"]`
- 链接 `返回邮箱状态` → `@L["OAuth.BackToStatus"]`

- [ ] **Step 8: 更新 `BuyerPageModelTests.cs` 与 `OAuthCallbackModelTests.cs`**

- `BuyerPageModelTests.cs`：
  - `new VerifyModel(<db>)` → `new VerifyModel(<db>, TestLocalizer.Shared)`
  - `new EmailModel(<db>, <ruleService>)` → `new EmailModel(<db>, <ruleService>, TestLocalizer.Shared)`
  - 若有断言中文 `ErrorMessage`，改为断言对应 key（如 `"Buyer.LinkInvalidOrExpired"`）。
- `OAuthCallbackModelTests.cs`：
  - `new CallbackModel(<db>, <ruleService>, <providers>)` → 末尾追加 `, TestLocalizer.Shared`。
  - 现有 `Assert.NotNull(model.ErrorMessage)` 无需改（仍非空）。

- [ ] **Step 9: 构建并测试**

Run: `dotnet build WebMail.sln`
Run: `dotnet test tests/WebMail.Tests`
Expected: 全绿。

- [ ] **Step 10: 提交**

```bash
git add src/WebMail/Services/BuyerRuleService.cs src/WebMail/Pages/Buyer src/WebMail/Pages/OAuth tests/WebMail.Tests/BuyerPageModelTests.cs tests/WebMail.Tests/OAuthCallbackModelTests.cs
git commit -m "feat(i18n): localize buyer verify/email and oauth callback pages"
```

---

## Task 10: 全量验证与回归

**Files:** 无新增（验证为主）。

- [ ] **Step 1: 残留中文扫描**

Run（应只剩注释、种子数据 `管理员`、`.resx` 的 zh-CN 值）：
```bash
LC_ALL=C.UTF-8 grep -rnP '[\x{4e00}-\x{9fff}]' src/WebMail --include=*.cshtml --include=*.cs | grep -v '/obj/' | grep -v 'Resources/SharedResource.zh-CN.resx'
```
Expected: 输出仅为 `.cs` 注释行、`IdentitySeeder` 的 `DisplayName = "管理员"`、`BuyerRuleService` 已无中文（已变 key）。逐条确认无遗漏的界面文字。

- [ ] **Step 2: 全量构建 + 测试**

Run: `dotnet build WebMail.sln`
Run: `dotnet test tests/WebMail.Tests`
Expected: 构建成功，全部测试通过。

- [ ] **Step 3: 手动双语冒烟**

Run: `dotnet run --project src/WebMail`
逐项确认（先英文后中文，用导航切换器）：
- 登录页、登录错误消息（错误密码/禁用账号）
- 管理后台：用户管理标题/表头/按钮/创建与重置消息；买家管理筛选/表头/审核与删除消息
- 销售后台：我的买家表头/删除消息
- 供应商后台：标签页（未处理/失败/完成 + 计数）、表格、状态徽章、邮件页、垃圾邮件徽章
- 买家邮箱页：状态标签、各操作按钮、无操作提示
- OAuth 回调：成功/失败消息
- 切换语言后刷新页面，语言保持（Cookie）；清除 Cookie 后由浏览器 `Accept-Language` 决定，未识别时回退英文。

- [ ] **Step 4: 提交（如有手动验证中发现的微调）**

```bash
git add -A
git commit -m "chore(i18n): final verification adjustments for en/zh-CN"
```

---

## Self-Review（计划对照 spec）

- **切换方式（Cookie + Accept-Language，默认 en）**：Task 1（`LocalizationConfig`/中间件）、Task 3（切换器）、Task 4（UI）覆盖。✓
- **范围 = 界面 + 系统消息**：Task 4–9 覆盖全部界面文字与页面模型/服务消息；明确排除邮件正文/业务数据/枚举显示/注释/种子数据（Global Constraints + Task 10 Step 1 守护）。✓
- **语言 en + zh-CN，默认 en**：`LocalizationConfig`（Task 1）+ `LocalizationConfigTests`。✓
- **官方本地化 + 共享资源**：`SharedResource` + 单一 `.resx`（Task 1–2）。✓
- **资源 key 一致性测试**：`SharedResourceParityTests`（Task 2）。✓
- **校验消息本地化**：`AddDataAnnotationsLocalization`（Task 1 Step 6）。注：当前页面模型无 `[Required]` 等数据注解（登录用手动校验），故实际校验消息已并入页面模型消息路径；接线保留以备后续注解使用。✓
- **防开放重定向**：切换器复用 `AuthRouting.IsLocalUrl`（Task 3）。✓
- **测试策略**：Task 1/2/3 提供单元测试；浏览器/Accept-Language 端到端解析由框架中间件保证并在 Task 10 手动冒烟（不引入 `Mvc.Testing` 新依赖，符合"不新增 NuGet"约束）。✓

**Placeholder 扫描**：无 TBD/TODO；每个代码步骤含完整代码或精确替换；资源目录给出全部真实译文。✓

**类型一致性**：`BuyerUnlinkBlockedMessage` → `BuyerUnlinkBlockedMessageKey` 的改名与全部 3 个调用方集中在 Task 9 Step 1/4/6，保证每个 Task 结束可编译（Task 8 已显式移除对 `BuyerRuleService` 的改动）。所有页面模型新增的 `IStringLocalizer<SharedResource> loc` 参数与测试构造点一一对应。✓
